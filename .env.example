# Top-level placeholder. The backend has its own .env (see backend/.env.example).
# v1 does NOT require any third-party API keys.
# Reserved slots if you later want to plug in:
#   OPENAI_API_KEY     - for an LLM-assisted lane assigner
#   HUGGINGFACE_TOKEN  - for hosted Demucs/ML model access
#   PLAUSIBLE_DOMAIN   - for shipping telemetry (Stage 6)
# Copy this file to .env if you set any of them, and never commit the populated one.
